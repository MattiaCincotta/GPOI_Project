services:
  gpoi-project:
    build: .
    container_name: gpoi-project
    restart: unless-stopped
    expose:
      - "6000:80"
    networks:
      - reverse-proxy

networks:
  reverse-proxy:
    external: true
